import React from "react";
import { render, screen } from "@testing-library/react";
import ImageLinkGrid from "./image-link-grid";

describe("ImageLinkGrid component", () => {
    it("should render circular images", () => {
        render(<ImageLinkGrid />);
        // TODO: Add tests
    });
});